All twelve repository userscripts received extended offline stress coverage: 100 route-return cycles per script, a 10,000-row DOM, simulated hidden/visible transitions and network conditions. Eleven SakaLuX scripts received actual repeated panel/public-switch interaction tests; all 23 Suite switches were exercised repeatedly and Chat search/maximize/mention recovery was checked. Chromium freezing/resuming was also exercised. Enhancer protected all 5,000 inventory-thumbnail rows in its dedicated load test. Eight API-bearing scripts received real-browser public-method tests with synthetic credentials and slow/offline/429/malformed/success responses. DOM-only tools have no equivalent API scenario.

Seven complete updated userscripts are included: Chat v1.2.20 bounds message history and restores mentions after toggles; Stocks v0.8.7 coalesces syncs; Elimination v1.3.44 cleans discarded observers and handlers; Market v1.17.40 coalesces price/equipment requests; Auditor v1.3.16 serializes rate permits and coalesces snapshots; Hub v1.9.73 coalesces registry refreshes and synchronizes current NEW metadata; Suite v0.9.929 cleans Member Travel Map and Target Alerts listeners.

Measured 20-caller API bursts: Stocks 60 → 3 requests, Market loadout 40 → 2, Hub 27 → 8, Auditor snapshot 1,840 → 92. Suite retained additional global listeners after 20 complete toggle cycles: 100 → 0; pending callbacks drain to zero after 60 seconds. Market also has a same-turn cached-read/forced-refresh regression.

Final-source CPU checks use 28 full before/after samples and 40 focused samples (five fresh-browser repetitions), actual 412px layout and six-times CPU throttling. No sampled JavaScript errors or tasks above 50ms. Focused medians: combined 358.13 → 345.21ms, native-enabled Suite 191.63 → 201.72ms, with overlapping sample ranges. This round improves request duplication and retained resources; it does not establish a CPU gain for every script.

The bundle contains all twelve complete userscripts, synchronized registry and documentation, measured test reports and regression harnesses. A separate ZIP contains complete previous versions of all seven modified scripts. SHA-256 checksums are included.

Memory measurements use real 30-minute wall-clock sessions with bounded synthetic chat, post-GC samples and two contexts: combined SakaLuX, and all 14 native Suite modules enabled. The long-run report records its tested versions; later changes confined to request-sharing or disable cleanup are additionally verified by focused browser/unit regressions. Auditor network fixtures scale delays only in the test copy; production 1,100ms request spacing and 3/6/12s retries remain intact and actual permit spacing is separately measured.

These are offline synthetic Chromium tests. They do not certify authenticated Torn behavior, actual TornPDA phone FPS, Android suspension or battery consumption. Full measured numbers and all assertions are in TEST-REPORT.md and the attached JSON reports.

Original Stocks 65ms return observations: 97/100. An explicit timed final-source retest verified 100/100 mounted-once returns, 99 early samples, max observed wall latency 98ms, without increasing production delays. The original timing finding is retained in results.json and resolved by stocks-route-verification.json. Thirty-minute warm-up-to-final heap: combined 8.30 → 8.55MB; native-enabled Suite 6.24 → 5.73MB; DOM, intervals and observer counts remained unchanged.
