name: Normalize installed versions
on:
  push:
    branches: [main]
    paths:
      - '*.user.js'
      - 'scripts.json'
      - 'greasyfork/*.md'
      - '.github/scripts/normalize_installed_versions.py'
      - '.github/scripts/fix_hub_launcher_font_audit.py'
      - '.github/workflows/normalize-installed-versions.yml'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: normalize-installed-versions-main
  cancel-in-progress: false
jobs:
  normalize:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Repair Hub launcher fallback and audit native Points/Merits fonts
        run: python3 .github/scripts/fix_hub_launcher_font_audit.py
      - name: Normalize module, Hub, NEW and release surfaces
        run: python3 .github/scripts/normalize_installed_versions.py
      - name: Validate JavaScript syntax
        run: |
          for f in *.user.js; do node --check "$f"; done
      - name: Commit normalization if needed
        run: |
          if git diff --quiet; then
            echo "Version and release surfaces already normalized."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- '*.user.js' scripts.json greasyfork/*.md FONT-SAFETY-AUDIT-2026-09-18.md
          git commit -m "Synchronize version, Hub launcher and font safety audit"
          git push
