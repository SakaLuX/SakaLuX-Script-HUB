name: Update Bazaar Smart Pricer v1.1.16
on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/update_bazaar_smart_pricer_v1116.py'
      - '.github/workflows/update-bazaar-smart-pricer-v1116.yml'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: bazaar-smart-pricer-v1116-main
  cancel-in-progress: false
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Apply v1.1.16 fifth-item arrow fix
        run: python3 .github/scripts/update_bazaar_smart_pricer_v1116.py
      - name: Validate userscript
        run: node --check SakaLuX-Bazaar-Smart-Pricer.user.js
      - name: Validate registry
        run: python3 -m json.tool scripts.json >/dev/null
      - name: Verify v1.1.16 surfaces
        run: |
          grep -F '// @version      1.1.16' SakaLuX-Bazaar-Smart-Pricer.user.js
          grep -F "|| '1.1.16';" SakaLuX-Bazaar-Smart-Pricer.user.js
          grep -F 'function findManageArrow(item)' SakaLuX-Bazaar-Smart-Pricer.user.js
          grep -F 'eye|view|preview|inspect|details' SakaLuX-Bazaar-Smart-Pricer.user.js
          ! grep -F 'Waiting 2s' SakaLuX-Bazaar-Smart-Pricer.user.js
          grep -F '## v1.1.16 — 2026-09-20' CHANGELOG-Bazaar-Smart-Pricer.md
          test -f releases/bazaar-smart-pricer-v1.1.16.md
      - name: Commit complete v1.1.16 update
        run: |
          if git diff --quiet && [ -z "$(git status --porcelain --untracked-files=all)" ]; then
            echo "Already updated."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add SakaLuX-Bazaar-Smart-Pricer.user.js scripts.json CHANGELOG-Bazaar-Smart-Pricer.md README-Bazaar-Smart-Pricer.md greasyfork/Bazaar-Smart-Pricer.md releases/bazaar-smart-pricer-v1.1.16.md backups/bazaar-smart-pricer-v1.1.15-2026-09-20
          git commit -m "Release Bazaar Smart Pricer v1.1.16 fifth-item arrow fix"
          git push
