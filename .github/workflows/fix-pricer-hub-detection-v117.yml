name: Fix Smart Pricer Hub detection v1.1.7
on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/fix_pricer_hub_detection_v117.py'
      - '.github/workflows/fix-pricer-hub-detection-v117.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply Smart Pricer Hub detection fix
        run: python3 .github/scripts/fix_pricer_hub_detection_v117.py
      - name: Synchronize registry, Hub fallback and GreasyFork docs
        run: python3 .github/scripts/normalize_installed_versions.py
      - name: Syntax check
        run: |
          node --check SakaLuX-Bazaar-Smart-Pricer.user.js
          node --check SakaLuX-Script-Hub.user.js
      - name: Commit synchronized fix
        run: |
          if git diff --quiet; then
            echo "No changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add SakaLuX-Bazaar-Smart-Pricer.user.js SakaLuX-Script-Hub.user.js scripts.json greasyfork/*.md
          git commit -m "Synchronize Smart Pricer Hub detection metadata"
          git push
