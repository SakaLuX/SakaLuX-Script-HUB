name: Patch Market Intelligence v1.15.11
on:
  push:
    branches: [main]
    paths:
      - '.github/patches/mi-v1.15.11.py'
      - '.github/workflows/patch-mi-v1.15.11.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python .github/patches/mi-v1.15.11.py
      - run: |
          git config user.name "SakaLuX"
          git config user.email "actions@users.noreply.github.com"
          git add SakaLuX-Market-Intelligence.user.js scripts.json UPDATE-INFO.md greasyfork/Market-Intelligence.md
          git diff --cached --quiet && exit 0
          git commit -m "Market Intelligence v1.15.11 Bazaar PDA card separation"
          git push
