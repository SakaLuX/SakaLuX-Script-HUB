name: One-time Hub resource launcher migration

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/hub-resource-launcher.yml'
      - '.migration/hub-resource-launcher-v2.py'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Apply migration
        run: python .migration/hub-resource-launcher-v2.py
      - name: Validate
        shell: bash
        run: |
          set -euo pipefail
          node --check SakaLuX-Script-Hub.user.js
          python -m json.tool scripts.json >/dev/null
      - name: Commit changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'SakaLuX'
          git config user.email 'sakalutzu92@yahoo.com'
          git add SakaLuX-Script-Hub.user.js greasyfork/Script-Hub.md
          if git diff --cached --quiet; then
            echo 'No migration changes.'
            exit 0
          fi
          git commit -m 'Hub v1.9.13 resource-bar launcher [skip ci]'
          git push
