name: Apply SakaLuX Suite v0.7.0 parity hardening
on:
  push:
    paths:
      - '.github/workflows/suite-v070-parity.yml'
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply v0.7.0 patch
        run: python tools/suite_v070_patch.py
      - name: Verify JavaScript syntax
        run: node --check SakaLuX-Suite.user.js
      - name: Commit v0.7.0
        run: |
          git config user.name "SakaLuX"
          git config user.email "sakalutzu92@yahoo.com"
          git add SakaLuX-Suite.user.js greasyfork/SakaLuX-Suite.md
          git commit -m "Harden SakaLuX Suite parity to v0.7.0"
          git push origin HEAD:main
