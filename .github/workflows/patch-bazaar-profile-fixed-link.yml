name: Patch Bazaar fixed profile link
on:
  push:
    branches: [main]
    paths:
      - '.github/patch-bazaar-profile-fixed-link.trigger'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: python .github/scripts/patch_bazaar_profile_fixed_link.py
      - name: Validate
        run: |
          grep -q '@version      5.3.25' SakaLuX-Bazaar-Thanker-PDA.user.js
          grep -q "const BAZAAR_VERSION='5.3.25';" SakaLuX-Bazaar-Thanker-PDA.user.js
          grep -q "https://www.torn.com/profiles.php?XID=2380374" SakaLuX-Bazaar-Thanker-PDA.user.js
          grep -q "'🙏'" SakaLuX-Bazaar-Thanker-PDA.user.js
          ! grep -A3 -B1 "const sellerLink = makeLink" SakaLuX-Bazaar-Thanker-PDA.user.js | grep -q "settings.sellerId"
          python -m json.tool scripts.json >/dev/null
          node --check SakaLuX-Bazaar-Thanker-PDA.user.js
      - name: Commit release
        run: |
          git config user.name 'SakaLuX'
          git config user.email 'sakalutzu92@yahoo.com'
          git add SakaLuX-Bazaar-Thanker-PDA.user.js scripts.json greasyfork/Bazaar-Thanker.md
          git commit -m 'Bazaar Thanker v5.3.25 fixed profile link'
          git push
