const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');
function gate(source){const match=/\n    (?:async )?function rateGate\(/.exec(source);assert(match);const end=source.indexOf('\n    function request(',match.index);return source.slice(match.index,end);}
async function run(source,label,gapMs=20,count=20){const context=vm.createContext({Promise,Date,Math,RATE:{minGapMs:gapMs,lastAt:0},sleep:ms=>new Promise(r=>setTimeout(r,ms))});vm.runInContext('let rateQueue=Promise.resolve();'+gate(source)+';globalThis.gate=rateGate',context);const times=[];await Promise.all(Array.from({length:count},async()=>{await context.gate();times.push(Date.now())}));const gaps=times.slice(1).map((time,index)=>time-times[index]);return {label,requests:count,testGapMs:gapMs,minGapMs:Math.min(...gaps),maxGapMs:Math.max(...gaps)};}
(async()=>{const before=await run(fs.readFileSync('backups/performance-audit-2026-09-18/SakaLuX-Account-Auditor.user.js','utf8'),'before');const current=fs.readFileSync('SakaLuX-Account-Auditor.user.js','utf8');const after=await run(current,'after');const production=await run(current,'production-gap',1100,4);fs.mkdirSync('reports/extended-performance-2026-09-18',{recursive:true});fs.writeFileSync('reports/extended-performance-2026-09-18/auditor-rate.json',JSON.stringify({before,after,production},null,2));console.log(JSON.stringify({before,after,production}));if(process.argv.includes('--assert')){assert(production.minGapMs>=1000,'Production 1100ms permits must remain spaced');assert(after.minGapMs>=15,'Concurrent callers must preserve rate spacing; test uses shortened 20ms gap, production remains 1100ms');}})().catch(e=>{console.error(e);process.exitCode=1});
